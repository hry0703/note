# Java 集合框架详解

## 📚 目录

1. [集合框架概述](#集合框架概述)
2. [Collection 接口](#collection-接口)
3. [List（列表）](#list列表)
4. [Set（集合）](#set集合)
5. [Map（映射）](#map映射)
6. [集合的选择指南](#集合的选择指南)
7. [常用操作和技巧](#常用操作和技巧)

---

## 集合框架概述

### 什么是集合框架？

Java 集合框架（Java Collections Framework）是 Java 提供的一套用于存储和操作对象集合的类和接口。

### 集合框架的层次结构

```
Collection（接口）
├── List（接口）- 有序，可重复
│   ├── ArrayList（动态数组）
│   ├── LinkedList（链表）
│   └── Vector（已过时，不推荐）
│
└── Set（接口）- 无序，不重复
    ├── HashSet（哈希表）
    ├── TreeSet（红黑树，有序）
    └── LinkedHashSet（链表+哈希，保持插入顺序）

Map（接口）- 键值对，独立于 Collection
├── HashMap（哈希表）
├── TreeMap（红黑树，有序）
└── LinkedHashMap（链表+哈希，保持插入顺序）
```

### 核心接口和类

| 接口/类 | 特点 | 是否有序 | 是否可重复 |
|---------|------|---------|-----------|
| **List** | 列表 | ✅ 有序 | ✅ 可重复 |
| **Set** | 集合 | ❌ 无序（部分有序） | ❌ 不重复 |
| **Map** | 映射（键值对） | ❌ 无序（部分有序） | 键不重复，值可重复 |

---

## Collection 接口

### Collection 接口的常用方法

```java
import java.util.Collection;

Collection<String> collection = new ArrayList<>();

// 添加元素
collection.add("元素1");
collection.addAll(otherCollection);  // 添加另一个集合的所有元素

// 删除元素
collection.remove("元素1");
collection.removeAll(otherCollection);  // 删除另一个集合的所有元素
collection.clear();  // 清空所有元素

// 查询
int size = collection.size();  // 获取元素个数
boolean isEmpty = collection.isEmpty();  // 是否为空
boolean contains = collection.contains("元素1");  // 是否包含
boolean containsAll = collection.containsAll(otherCollection);  // 是否包含所有

// 遍历
for (String item : collection) {
    System.out.println(item);
}

// 转换为数组
Object[] array = collection.toArray();
String[] strArray = collection.toArray(new String[0]);
```

---

## List（列表）

### List 的特点

- ✅ **有序**：元素按照插入顺序排列
- ✅ **可重复**：同一个元素可以出现多次
- ✅ **有索引**：可以通过索引访问元素（从 0 开始）

### List 接口的常用方法

```java
import java.util.List;
import java.util.ArrayList;

List<String> list = new ArrayList<>();

// 添加元素
list.add("苹果");              // 添加到末尾
list.add(0, "香蕉");           // 在指定位置插入
list.addAll(otherList);        // 添加另一个集合

// 获取元素
String item = list.get(0);     // 获取指定位置的元素
int size = list.size();        // 获取元素个数

// 查找元素
int index = list.indexOf("苹果");      // 查找第一次出现的索引
int lastIndex = list.lastIndexOf("苹果");  // 查找最后一次出现的索引
boolean contains = list.contains("苹果");   // 是否包含

// 修改元素
list.set(0, "橙子");           // 替换指定位置的元素

// 删除元素
list.remove(0);                // 删除指定位置的元素
list.remove("苹果");           // 删除指定元素（删除第一个匹配的）
list.clear();                  // 清空所有元素

// 截取子列表
List<String> subList = list.subList(0, 2);  // 获取索引 0-1 的子列表

// 遍历
for (int i = 0; i < list.size(); i++) {
    System.out.println(list.get(i));
}

for (String item : list) {
    System.out.println(item);
}

list.forEach(item -> System.out.println(item));  // Java 8+ Lambda
```

### List 的实现类

#### 1. ArrayList（动态数组）

**特点**：
- 底层使用数组实现
- 随机访问快（O(1)）
- 插入和删除较慢（需要移动元素）
- 线程不安全

**使用场景**：
- 查询操作多
- 需要随机访问
- 最常用的 List 实现

**示例**：
```java
List<String> list = new ArrayList<>();
list.add("苹果");
list.add("香蕉");
list.add("橙子");

// 快速随机访问
String first = list.get(0);  // O(1)
```

#### 2. LinkedList（链表）

**特点**：
- 底层使用双向链表实现
- 插入和删除快（O(1)）
- 随机访问慢（O(n)）
- 线程不安全

**使用场景**：
- 频繁插入和删除
- 不需要随机访问

**示例**：
```java
List<String> list = new LinkedList<>();
list.add("苹果");
list.add("香蕉");

// 在头部插入
list.add(0, "橙子");  // 很快

// 在中间插入
list.add(1, "葡萄");  // 很快
```

#### 3. Vector（已过时，不推荐）

**特点**：
- 线程安全（同步）
- 性能较差
- 已过时，不推荐使用

**替代方案**：
- 使用 `ArrayList` + `Collections.synchronizedList()` 实现线程安全
- 或使用 `CopyOnWriteArrayList`（并发场景）

---

## Set（集合）

### Set 的特点

- ❌ **无序**：不保证元素的顺序（部分实现类有序）
- ❌ **不重复**：同一个元素只能出现一次
- ❌ **无索引**：不能通过索引访问元素

### Set 接口的常用方法

```java
import java.util.Set;
import java.util.HashSet;

Set<String> set = new HashSet<>();

// 添加元素
set.add("苹果");
set.add("香蕉");
set.add("苹果");  // 重复，不会添加，返回 false

// 删除元素
set.remove("苹果");
set.clear();  // 清空

// 查询
int size = set.size();
boolean isEmpty = set.isEmpty();
boolean contains = set.contains("苹果");

// 遍历（只能使用增强 for 循环或迭代器）
for (String item : set) {
    System.out.println(item);
}

set.forEach(item -> System.out.println(item));  // Java 8+ Lambda
```

### Set 的实现类

#### 1. HashSet（哈希表）

**特点**：
- 底层使用哈希表（HashMap）实现
- 无序（不保证顺序）
- 查找、插入、删除都很快（O(1) 平均）
- 线程不安全

**使用场景**：
- 需要快速查找
- 不需要排序
- 最常用的 Set 实现

**示例**：
```java
Set<String> set = new HashSet<>();
set.add("苹果");
set.add("香蕉");
set.add("橙子");

// 输出顺序不确定
System.out.println(set);  // 可能是：[香蕉, 苹果, 橙子]
```

**注意事项**：
- 存储自定义对象时，需要重写 `equals()` 和 `hashCode()` 方法

#### 2. TreeSet（红黑树）

**特点**：
- 底层使用红黑树实现
- 自动排序（自然顺序或自定义比较器）
- 有序
- 查找、插入、删除稍慢（O(log n)）
- 线程不安全

**使用场景**：
- 需要排序
- 需要范围查询

**示例**：
```java
Set<String> set = new TreeSet<>();
set.add("苹果");
set.add("香蕉");
set.add("橙子");

// 自动排序
System.out.println(set);  // 输出：[橙子, 苹果, 香蕉]（按字母顺序）

// 自定义排序
Set<Integer> numbers = new TreeSet<>((a, b) -> b - a);  // 降序
numbers.add(3);
numbers.add(1);
numbers.add(2);
System.out.println(numbers);  // 输出：[3, 2, 1]
```

#### 3. LinkedHashSet（链表+哈希）

**特点**：
- 底层使用链表+哈希表实现
- 保持插入顺序
- 查找、插入、删除都很快（O(1) 平均）
- 线程不安全

**使用场景**：
- 需要保持插入顺序
- 需要快速查找

**示例**：
```java
Set<String> set = new LinkedHashSet<>();
set.add("苹果");
set.add("香蕉");
set.add("橙子");

// 保持插入顺序
System.out.println(set);  // 输出：[苹果, 香蕉, 橙子]
```

---

## Map（映射）

### Map 的特点

- 存储**键值对**（Key-Value）
- 键（Key）不重复，值（Value）可以重复
- 一个键对应一个值

### Map 接口的常用方法

```java
import java.util.Map;
import java.util.HashMap;

Map<String, Integer> map = new HashMap<>();

// 添加/修改元素
map.put("苹果", 10);        // 添加键值对
map.put("香蕉", 20);
map.put("苹果", 15);       // 如果键已存在，会覆盖原值

// 获取元素
Integer count = map.get("苹果");  // 获取值，如果键不存在返回 null
Integer count2 = map.getOrDefault("橙子", 0);  // 如果键不存在，返回默认值

// 删除元素
map.remove("苹果");        // 删除指定键
map.clear();               // 清空所有元素

// 查询
int size = map.size();
boolean isEmpty = map.isEmpty();
boolean hasKey = map.containsKey("苹果");    // 是否包含键
boolean hasValue = map.containsValue(10);     // 是否包含值

// 获取所有键或值
Set<String> keys = map.keySet();            // 获取所有键（Set）
Collection<Integer> values = map.values();  // 获取所有值（Collection）

// 遍历
// 方式 1：遍历键
for (String key : map.keySet()) {
    Integer value = map.get(key);
    System.out.println(key + ": " + value);
}

// 方式 2：遍历键值对（推荐）
for (Map.Entry<String, Integer> entry : map.entrySet()) {
    String key = entry.getKey();
    Integer value = entry.getValue();
    System.out.println(key + ": " + value);
}

// 方式 3：Java 8+ Lambda
map.forEach((key, value) -> System.out.println(key + ": " + value));
```

### Map 的实现类

#### 1. HashMap（哈希表）

**特点**：
- 底层使用哈希表实现
- 无序（不保证顺序）
- 查找、插入、删除都很快（O(1) 平均）
- 允许 null 键和 null 值
- 线程不安全

**使用场景**：
- 需要快速查找
- 不需要排序
- 最常用的 Map 实现

**示例**：
```java
Map<String, Integer> map = new HashMap<>();
map.put("苹果", 10);
map.put("香蕉", 20);
map.put("橙子", 15);

// 输出顺序不确定
System.out.println(map);  // 可能是：{香蕉=20, 苹果=10, 橙子=15}
```

#### 2. TreeMap（红黑树）

**特点**：
- 底层使用红黑树实现
- 自动排序（根据键的自然顺序或比较器）
- 有序
- 查找、插入、删除稍慢（O(log n)）
- 不允许 null 键（允许 null 值）
- 线程不安全

**使用场景**：
- 需要排序
- 需要范围查询

**示例**：
```java
Map<String, Integer> map = new TreeMap<>();
map.put("苹果", 10);
map.put("香蕉", 20);
map.put("橙子", 15);

// 自动排序（按键排序）
System.out.println(map);  // 输出：{橙子=15, 苹果=10, 香蕉=20}（按字母顺序）

// 自定义排序
Map<Integer, String> numbers = new TreeMap<>((a, b) -> b - a);  // 降序
numbers.put(3, "三");
numbers.put(1, "一");
numbers.put(2, "二");
System.out.println(numbers);  // 输出：{3=三, 2=二, 1=一}
```

#### 3. LinkedHashMap（链表+哈希）

**特点**：
- 底层使用链表+哈希表实现
- 保持插入顺序
- 查找、插入、删除都很快（O(1) 平均）
- 允许 null 键和 null 值
- 线程不安全

**使用场景**：
- 需要保持插入顺序
- 需要快速查找

**示例**：
```java
Map<String, Integer> map = new LinkedHashMap<>();
map.put("苹果", 10);
map.put("香蕉", 20);
map.put("橙子", 15);

// 保持插入顺序
System.out.println(map);  // 输出：{苹果=10, 香蕉=20, 橙子=15}
```

---

## 集合的选择指南

### 如何选择合适的集合？

#### 1. 需要有序 + 可重复？
→ 使用 **List**
- 查询多 → `ArrayList`
- 插入删除多 → `LinkedList`

#### 2. 需要不重复？
→ 使用 **Set**
- 不需要排序 → `HashSet`
- 需要排序 → `TreeSet`
- 需要保持插入顺序 → `LinkedHashSet`

#### 3. 需要键值对？
→ 使用 **Map**
- 不需要排序 → `HashMap`
- 需要排序 → `TreeMap`
- 需要保持插入顺序 → `LinkedHashMap`

### 性能对比

| 操作 | ArrayList | LinkedList | HashSet | TreeSet | HashMap | TreeMap |
|------|-----------|------------|---------|---------|---------|---------|
| **查找** | O(1) | O(n) | O(1) | O(log n) | O(1) | O(log n) |
| **插入** | O(n) | O(1) | O(1) | O(log n) | O(1) | O(log n) |
| **删除** | O(n) | O(1) | O(1) | O(log n) | O(1) | O(log n) |
| **有序** | ✅ | ✅ | ❌ | ✅ | ❌ | ✅ |

---

## 常用操作和技巧

### 1. 集合初始化

```java
// 方式 1：逐个添加
List<String> list = new ArrayList<>();
list.add("苹果");
list.add("香蕉");

// 方式 2：使用 Arrays.asList（Java 9+）
List<String> list2 = new ArrayList<>(Arrays.asList("苹果", "香蕉", "橙子"));

// 方式 3：Java 9+ List.of（不可变）
List<String> list3 = List.of("苹果", "香蕉", "橙子");

// 方式 4：Java 9+ Map.of（不可变）
Map<String, Integer> map = Map.of("苹果", 10, "香蕉", 20);
```

### 2. 集合转换

```java
// List 转 Set（去重）
List<String> list = Arrays.asList("苹果", "香蕉", "苹果");
Set<String> set = new HashSet<>(list);  // 自动去重

// Set 转 List
List<String> list2 = new ArrayList<>(set);

// List 转数组
String[] array = list.toArray(new String[0]);

// 数组转 List
List<String> list3 = Arrays.asList(array);
```

### 3. 集合排序

```java
// List 排序
List<String> list = new ArrayList<>(Arrays.asList("香蕉", "苹果", "橙子"));
Collections.sort(list);  // 自然排序
Collections.sort(list, (a, b) -> b.compareTo(a));  // 自定义排序（降序）

// Java 8+ Stream 排序
list.sort((a, b) -> a.compareTo(b));  // 升序
list.sort(Comparator.reverseOrder());  // 降序
```

### 4. 集合过滤和转换（Java 8+）

```java
List<String> list = Arrays.asList("苹果", "香蕉", "橙子", "葡萄");

// 过滤
List<String> filtered = list.stream()
    .filter(item -> item.length() == 2)
    .collect(Collectors.toList());

// 转换
List<String> upper = list.stream()
    .map(String::toUpperCase)
    .collect(Collectors.toList());

// 去重
List<String> distinct = list.stream()
    .distinct()
    .collect(Collectors.toList());
```

### 5. 集合判空和大小

```java
// 推荐：使用 isEmpty() 而不是 size() == 0
if (list.isEmpty()) {
    System.out.println("列表为空");
}

// 不推荐
if (list.size() == 0) {  // 性能稍差
    System.out.println("列表为空");
}
```

### 6. 集合遍历的最佳实践

```java
// 方式 1：增强 for 循环（推荐，简单明了）
for (String item : list) {
    System.out.println(item);
}

// 方式 2：Java 8+ forEach（推荐，函数式风格）
list.forEach(item -> System.out.println(item));
list.forEach(System.out::println);  // 方法引用

// 方式 3：传统 for 循环（需要索引时使用）
for (int i = 0; i < list.size(); i++) {
    System.out.println(list.get(i));
}

// Map 遍历（推荐使用 entrySet）
for (Map.Entry<String, Integer> entry : map.entrySet()) {
    System.out.println(entry.getKey() + ": " + entry.getValue());
}

// Map 遍历（Java 8+）
map.forEach((key, value) -> System.out.println(key + ": " + value));
```

### 7. 线程安全的集合

```java
// 方式 1：使用 Collections.synchronizedXXX
List<String> syncList = Collections.synchronizedList(new ArrayList<>());
Set<String> syncSet = Collections.synchronizedSet(new HashSet<>());
Map<String, Integer> syncMap = Collections.synchronizedMap(new HashMap<>());

// 方式 2：使用并发集合（推荐）
import java.util.concurrent.*;

List<String> concurrentList = new CopyOnWriteArrayList<>();
Set<String> concurrentSet = new ConcurrentHashMap.newKeySet();
Map<String, Integer> concurrentMap = new ConcurrentHashMap<>();
```

---

## 📝 总结

### 快速选择表

| 需求 | 推荐集合 |
|------|---------|
| 有序 + 可重复 + 查询多 | `ArrayList` |
| 有序 + 可重复 + 插入删除多 | `LinkedList` |
| 不重复 + 不需要排序 | `HashSet` |
| 不重复 + 需要排序 | `TreeSet` |
| 不重复 + 保持插入顺序 | `LinkedHashSet` |
| 键值对 + 不需要排序 | `HashMap` |
| 键值对 + 需要排序 | `TreeMap` |
| 键值对 + 保持插入顺序 | `LinkedHashMap` |

### 最佳实践

1. **优先使用接口类型**：`List<String> list = new ArrayList<>();`
2. **选择合适的实现类**：根据使用场景选择
3. **注意线程安全**：多线程环境使用并发集合
4. **合理使用泛型**：避免类型转换错误
5. **注意 null 值**：某些集合不允许 null 值

---

*最后更新：2024年*
