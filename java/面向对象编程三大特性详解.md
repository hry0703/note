# Java 面向对象编程三大特性详解

## 📚 目录
1. [封装（Encapsulation）](#1-封装encapsulation)
2. [继承（Inheritance）](#2-继承inheritance)
3. [多态（Polymorphism）](#3-多态polymorphism)
4. [三大特性的关系](#三大特性的关系)
5. [实际应用场景](#实际应用场景)

---

## 1. 封装（Encapsulation）

### 1.1 什么是封装？

**封装**是将数据（属性）和操作数据的方法（行为）包装在一起，隐藏内部实现细节，只暴露必要的接口给外部使用。

### 1.2 封装的核心思想

- ✅ **数据隐藏**：将属性设为私有（private），外部不能直接访问
- ✅ **访问控制**：通过公共方法（getter/setter）控制对属性的访问
- ✅ **数据验证**：在 setter 方法中添加验证逻辑，确保数据有效性
- ✅ **接口暴露**：只暴露必要的方法给外部使用

### 1.3 访问修饰符

| 修饰符 | 同类 | 同包 | 子类 | 不同包 |
|--------|------|------|------|--------|
| `private` | ✅ | ❌ | ❌ | ❌ |
| `default`（包访问） | ✅ | ✅ | ❌ | ❌ |
| `protected` | ✅ | ✅ | ✅ | ❌ |
| `public` | ✅ | ✅ | ✅ | ✅ |

### 1.4 封装示例

```java
class Student {
    // 私有属性：外部不能直接访问
    private String name;
    private int age;
    
    // 构造方法
    public Student(String name, int age) {
        this.name = name;
        if (age > 0 && age < 150) {
            this.age = age;
        }
    }
    
    // Getter 方法：提供只读访问
    public String getName() {
        return name;
    }
    
    public int getAge() {
        return age;
    }
    
    // Setter 方法：提供受控的修改
    public void setAge(int age) {
        // 封装的好处：可以添加验证逻辑
        if (age > 0 && age < 150) {
            this.age = age;
        } else {
            System.out.println("年龄无效！");
        }
    }
}
```

### 1.5 封装的优势

1. **数据安全**：防止外部直接修改数据，避免数据被非法访问
2. **数据验证**：在 setter 中可以添加验证，确保数据有效性
3. **代码维护**：修改内部实现不影响外部调用
4. **接口清晰**：只暴露必要的方法，接口更清晰

---

## 2. 继承（Inheritance）

### 2.1 什么是继承？

**继承**是面向对象编程中一个类可以继承另一个类的属性和方法，实现代码复用和层次化设计。

### 2.2 继承的核心概念

- ✅ **父类（超类）**：被继承的类，也叫基类
- ✅ **子类（派生类）**：继承父类的类
- ✅ **代码复用**：子类可以复用父类的代码
- ✅ **方法重写**：子类可以重写父类的方法

### 2.3 继承的语法

```java
// 父类
class Animal {
    protected String name;
    
    public void eat() {
        System.out.println("正在吃东西...");
    }
}

// 子类继承父类
class Dog extends Animal {
    // 子类自动拥有父类的属性和方法
    public void bark() {
        System.out.println("汪汪汪！");
    }
}
```

### 2.4 继承的特点

1. **单继承**：Java 只支持单继承，一个类只能继承一个父类
2. **多层继承**：可以形成继承链（A → B → C）
3. **方法重写**：子类可以重写父类的方法（使用 `@Override` 注解）
4. **super 关键字**：用于调用父类的构造方法或方法

### 2.5 继承示例

```java
// 父类
class Animal {
    protected String name;
    protected int age;
    
    public Animal(String name, int age) {
        this.name = name;
        this.age = age;
    }
    
    public void eat() {
        System.out.println(name + " 正在吃东西...");
    }
    
    public void makeSound() {
        System.out.println("发出声音...");
    }
}

// 子类：Dog
class Dog extends Animal {
    public Dog(String name, int age) {
        super(name, age); // 调用父类构造方法
    }
    
    // 重写父类方法
    @Override
    public void makeSound() {
        System.out.println(name + " 汪汪汪！");
    }
    
    // 子类特有的方法
    public void bark() {
        makeSound();
    }
}
```

### 2.6 继承的优势

1. **代码复用**：避免重复编写相同的代码
2. **层次化设计**：可以建立清晰的类层次结构
3. **易于维护**：修改父类，所有子类自动继承修改
4. **扩展性强**：子类可以添加新功能

### 2.7 继承的注意事项

- ⚠️ **不要过度使用继承**：继承关系应该是 "is-a" 关系（狗是动物）
- ⚠️ **优先使用组合**：如果只是 "has-a" 关系，使用组合而不是继承
- ⚠️ **避免深层继承**：继承层次不要太深，一般不超过 3-4 层

---

## 3. 多态（Polymorphism）

### 3.1 什么是多态？

**多态**是指同一个接口可以有不同的实现方式，或者同一个方法在不同对象上有不同的行为。

### 3.2 多态的类型

#### 3.2.1 编译时多态（方法重载）

同一个类中，方法名相同但参数不同：

```java
class Calculator {
    // 方法重载：参数类型不同
    public int add(int a, int b) {
        return a + b;
    }
    
    public double add(double a, double b) {
        return a + b;
    }
    
    public int add(int a, int b, int c) {
        return a + b + c;
    }
}
```

#### 3.2.2 运行时多态（方法重写）

父类引用指向子类对象，调用的是子类重写的方法：

```java
// 父类引用指向子类对象
Animal animal1 = new Dog("旺财", 3);
Animal animal2 = new Cat("小花", 2);

// 运行时根据实际对象类型调用相应的方法
animal1.makeSound(); // 调用 Dog 的 makeSound()
animal2.makeSound(); // 调用 Cat 的 makeSound()
```

### 3.3 多态的实现方式

#### 方式 1：继承 + 方法重写

```java
class Animal {
    public void makeSound() {
        System.out.println("发出声音...");
    }
}

class Dog extends Animal {
    @Override
    public void makeSound() {
        System.out.println("汪汪汪！");
    }
}

class Cat extends Animal {
    @Override
    public void makeSound() {
        System.out.println("喵喵喵！");
    }
}

// 使用多态
Animal[] animals = {new Dog(), new Cat()};
for (Animal animal : animals) {
    animal.makeSound(); // 每个对象调用自己的实现
}
```

#### 方式 2：接口实现

```java
interface Shape {
    double calculateArea();
}

class Circle implements Shape {
    private double radius;
    
    @Override
    public double calculateArea() {
        return Math.PI * radius * radius;
    }
}

class Rectangle implements Shape {
    private double width, height;
    
    @Override
    public double calculateArea() {
        return width * height;
    }
}

// 使用多态
Shape[] shapes = {new Circle(5), new Rectangle(3, 4)};
for (Shape shape : shapes) {
    System.out.println("面积：" + shape.calculateArea());
}
```

### 3.4 多态的优势

1. **代码灵活性**：可以统一处理不同类型的对象
2. **可扩展性**：添加新的子类不需要修改现有代码
3. **代码简洁**：减少 if-else 判断，代码更简洁
4. **符合开闭原则**：对扩展开放，对修改关闭

### 3.5 多态的应用场景

- **集合框架**：`List<String> list = new ArrayList<>();`
- **框架设计**：Spring 框架中的依赖注入
- **回调机制**：事件处理、监听器模式

---

## 三大特性的关系

### 关系图

```
封装 ──→ 继承 ──→ 多态
  ↓        ↓        ↓
数据隐藏  代码复用  灵活扩展
```

### 综合示例

```java
// 1. 封装：私有属性 + 公共方法
class Animal {
    private String name;  // 封装
    
    public Animal(String name) {
        this.name = name;
    }
    
    public String getName() {
        return name;
    }
}

// 2. 继承：Dog 继承 Animal
class Dog extends Animal {
    public Dog(String name) {
        super(name);
    }
    
    // 3. 多态：重写方法
    @Override
    public void makeSound() {
        System.out.println("汪汪汪！");
    }
}

// 使用多态
Animal animal = new Dog("旺财");
animal.makeSound(); // 运行时多态
```

---

## 实际应用场景

### 场景 1：用户管理系统

```java
// 封装：用户信息
class User {
    private String username;
    private String password;
    
    public void setPassword(String password) {
        // 验证密码强度
        if (password.length() >= 8) {
            this.password = password;
        }
    }
}

// 继承：不同类型的用户
class Admin extends User {
    public void manageSystem() {
        // 管理员特有功能
    }
}

class Customer extends User {
    public void placeOrder() {
        // 客户特有功能
    }
}

// 多态：统一处理
User[] users = {new Admin(), new Customer()};
for (User user : users) {
    user.login(); // 每个用户有自己的登录逻辑
}
```

### 场景 2：支付系统

```java
// 接口定义（多态的基础）
interface Payment {
    void pay(double amount);
}

// 不同支付方式实现接口
class Alipay implements Payment {
    @Override
    public void pay(double amount) {
        System.out.println("使用支付宝支付：" + amount);
    }
}

class WeChatPay implements Payment {
    @Override
    public void pay(double amount) {
        System.out.println("使用微信支付：" + amount);
    }
}

// 使用多态
Payment payment = new Alipay();
payment.pay(100.0); // 可以轻松切换支付方式
```

---

## 📝 总结

### 三大特性对比

| 特性 | 核心思想 | 主要作用 | 关键字 |
|------|---------|---------|--------|
| **封装** | 隐藏实现细节 | 数据安全、代码维护 | `private`, `public`, `getter/setter` |
| **继承** | 代码复用 | 建立类层次结构 | `extends`, `super` |
| **多态** | 同一接口不同实现 | 灵活扩展、统一处理 | `@Override`, `interface`, `implements` |

### 学习建议

1. **理解概念**：先理解每个特性的核心思想
2. **动手实践**：编写代码示例，加深理解
3. **观察应用**：在实际项目中观察这些特性的使用
4. **综合运用**：学会将三大特性结合起来使用

### 常见问题

**Q: 什么时候用继承，什么时候用接口？**
- 继承：用于 "is-a" 关系，有共同的属性和行为
- 接口：用于定义规范，实现 "can-do" 关系

**Q: 多态一定要用继承吗？**
- 不一定，接口也可以实现多态，而且更灵活

**Q: 封装是不是就是 private？**
- 封装是一种思想，private 是实现封装的手段之一

---

*最后更新：2024年*
