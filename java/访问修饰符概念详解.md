# Java 访问修饰符概念详解

## 📚 核心概念

在理解访问修饰符之前，需要先理解四个关键概念：

1. **同类**（Same Class）
2. **同包**（Same Package）
3. **子类**（Subclass）
4. **不同包**（Different Package）

---

## 1. 同类（Same Class）

### 定义
**同类**是指**同一个类内部**，即类自己的成员访问自己的其他成员。

### 特点
- ✅ 可以访问所有访问修饰符的成员（包括 `private`）
- ✅ 这是最宽松的访问权限

### 示例

```java
class Parent {
    private String privateField = "private";
    String defaultField = "default";
    protected String protectedField = "protected";
    public String publicField = "public";
    
    // 同类访问：在同一个类内部
    public void testSameClass() {
        // ✅ 所有修饰符都可以访问
        System.out.println(privateField);    // ✅ 可以访问
        System.out.println(defaultField);     // ✅ 可以访问
        System.out.println(protectedField);   // ✅ 可以访问
        System.out.println(publicField);    // ✅ 可以访问
    }
}
```

---

## 2. 同包（Same Package）

### 定义
**同包**是指**在同一个包（package）中的类**。

### 包的概念
- 包（package）是 Java 中组织类的方式
- 包名通常用点号分隔，如：`com.hry.firstjava`
- 同一个包中的类可以互相访问 `default` 和 `protected` 成员

### 示例

```java
// 文件 1：Parent.java
package com.hry.firstjava;  // 包名

class Parent {
    String defaultField = "default";      // 同包可以访问
    protected String protectedField = "protected";  // 同包可以访问
    public String publicField = "public";  // 任何地方都可以访问
}

// 文件 2：SamePackageClass.java
package com.hry.firstjava;  // 同一个包

class SamePackageClass {
    public void test() {
        Parent parent = new Parent();
        // ✅ 同包可以访问 default 和 protected
        System.out.println(parent.defaultField);     // ✅ 可以访问
        System.out.println(parent.protectedField);   // ✅ 可以访问
        System.out.println(parent.publicField);      // ✅ 可以访问
    }
}
```

### 同包的两种情况

#### 情况 1：同包非子类
- 在同一个包中
- **不是**子类关系
- 可以访问：`default`、`protected`、`public`

#### 情况 2：同包子类
- 在同一个包中
- **是**子类关系（继承）
- 可以访问：`default`、`protected`、`public`
- 注意：同包子类可以访问 `protected`，但这是因为"同包"，而不是因为"子类"

---

## 3. 子类（Subclass）

### 定义
**子类**是指**继承自父类的类**，使用 `extends` 关键字。

### 特点
- 子类可以继承父类的非私有成员
- 子类可以重写（override）父类的方法
- 子类可以添加新的成员

### 子类的两种情况

#### 情况 1：同包子类
```java
// 同一个包：com.hry.firstjava
class Parent {
    protected String protectedField = "protected";
}

class SamePackageSubclass extends Parent {
    // 同包子类：既是子类，又在同一个包中
    public void test() {
        // ✅ 可以访问 protected（因为同包）
        System.out.println(protectedField);
    }
}
```

#### 情况 2：不同包子类
```java
// 包 1：com.hry.firstjava
package com.hry.firstjava;
public class Parent {
    protected String protectedField = "protected";
}

// 包 2：com.hry.other
package com.hry.other;
import com.hry.firstjava.Parent;

class DifferentPackageSubclass extends Parent {
    // 不同包子类：是子类，但在不同的包中
    public void test() {
        // ✅ 可以访问 protected（因为子类）
        System.out.println(protectedField);
    }
}
```

### 重要说明
- **不同包子类**可以访问 `protected` 成员
- **不同包非子类**不能访问 `protected` 成员
- 这就是为什么表格中"子类"列显示 `protected` 可以访问的原因

---

## 4. 不同包（Different Package）

### 定义
**不同包**是指**在不同的包中的类**，且**不是子类关系**。

### 特点
- ❌ 不能访问 `private`（任何地方都不能）
- ❌ 不能访问 `default`（包访问权限）
- ❌ 不能访问 `protected`（除非是子类）
- ✅ 只能访问 `public`

### 示例

```java
// 包 1：com.hry.firstjava
package com.hry.firstjava;
public class Parent {
    private String privateField = "private";
    String defaultField = "default";
    protected String protectedField = "protected";
    public String publicField = "public";
}

// 包 2：com.hry.other
package com.hry.other;
import com.hry.firstjava.Parent;

class DifferentPackageClass {
    public void test() {
        Parent parent = new Parent();
        
        // ❌ private：不能访问
        // System.out.println(parent.privateField);
        
        // ❌ default：不能访问（不同包）
        // System.out.println(parent.defaultField);
        
        // ❌ protected：不能访问（不同包且不是子类）
        // System.out.println(parent.protectedField);
        
        // ✅ public：可以访问
        System.out.println(parent.publicField);  // ✅ 可以访问
    }
}
```

---

## 📊 访问权限总结表

| 修饰符 | 同类 | 同包非子类 | 同包子类 | 不同包子类 | 不同包非子类 |
|--------|------|-----------|---------|-----------|------------|
| `private` | ✅ | ❌ | ❌ | ❌ | ❌ |
| `default` | ✅ | ✅ | ✅ | ❌ | ❌ |
| `protected` | ✅ | ✅ | ✅ | ✅ | ❌ |
| `public` | ✅ | ✅ | ✅ | ✅ | ✅ |

### 详细说明

1. **同类**：同一个类内部，可以访问所有修饰符
2. **同包非子类**：同一个包中，但不是子类，可以访问 `default`、`protected`、`public`
3. **同包子类**：同一个包中，且是子类，可以访问 `default`、`protected`、`public`
4. **不同包子类**：不同包中，但是子类，可以访问 `protected`、`public`
5. **不同包非子类**：不同包中，且不是子类，只能访问 `public`

---

## 🔍 关键理解点

### 1. "同包"和"子类"是两个独立的维度

- **同包**：看的是包名是否相同
- **子类**：看的是是否有继承关系

### 2. `protected` 的特殊性

`protected` 的访问规则：
- ✅ 同类可以访问
- ✅ 同包可以访问（无论是否是子类）
- ✅ 不同包子类可以访问
- ❌ 不同包非子类不能访问

**记忆技巧**：`protected` = `default` + 不同包子类

### 3. `default`（包访问权限）

- 没有写任何访问修饰符就是 `default`
- 只能在同一个包中访问
- 不同包不能访问（即使是子类也不行）

### 4. 实际应用建议

- **`private`**：只在类内部使用，完全隐藏实现细节
- **`default`**：包内可见，用于包级别的封装
- **`protected`**：用于继承体系，子类可以访问
- **`public`**：对外接口，任何地方都可以访问

---

## 💡 记忆口诀

```
同类：全部可以
同包：default + protected + public
子类：protected + public（无论是否同包）
不同包：只有 public
```

---

## 📝 示例代码

完整的示例代码请查看：
- `java-project/src/main/java/com/hry/firstjava/AccessModifierExample.java`

运行示例：
```bash
cd java-project
mvn compile exec:java -Dexec.mainClass="com.hry.firstjava.AccessModifierExample"
```

---

*最后更新：2024年*
