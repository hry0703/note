# 文档问答 - 前端

Vue 3 + Vite 项目，对接 RAG Agent 后端。

## 启动

```bash
# 安装依赖
npm install

# 开发模式（需先启动后端 http://localhost:8002）
npm run dev
```

访问 http://localhost:5173

## 构建

```bash
npm run build
```

输出在 `dist/`，可部署到任意静态托管。
