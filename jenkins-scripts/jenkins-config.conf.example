# Jenkins Docker 部署配置文件
# 复制此文件为 jenkins-config.conf 并根据需要修改

# ===========================================
# 数据目录配置
# ===========================================

# Jenkins 数据存储根目录
# 推荐路径：
#   - 服务器部署: /opt/jenkins-data (推荐)
#   - 用户目录: /Users/$USER/jenkins-data 或 /home/$USER/jenkins-data
DATA_DIR="/opt/jenkins-data"

# ===========================================
# 数据目录结构
# ===========================================

# $DATA_DIR/
# └── jenkins/                   # Jenkins 数据
#     ├── home/                  # Jenkins 主目录
#     ├── docker-certs-ca/       # Docker CA 证书
#     ├── docker-certs-client/   # Docker 客户端证书
#     └── agent-workdir/         # 代理工作目录

# ===========================================
# 注意事项
# ===========================================

# 1. 确保有足够磁盘空间（建议 20GB+）
# 2. 确保目录权限正确 (UID 1000)
# 3. 定期备份 Jenkins 配置和任务数据
