# 机器学习基础

## 📋 什么是机器学习？

**机器学习（Machine Learning）**是人工智能的一个分支，让计算机能够从数据中学习，而不需要显式编程。

## 🎯 核心思想

```
数据 → 学习算法 → 模型 → 预测/决策
```

## 📚 机器学习类型

### 1. 监督学习（Supervised Learning）

**定义**：使用带标签的数据进行训练

**任务类型**：
- **分类（Classification）**：预测离散标签
  - 垃圾邮件检测
  - 图像分类
  - 情感分析
- **回归（Regression）**：预测连续值
  - 房价预测
  - 股票价格预测
  - 温度预测

**示例**：
```python
# 训练数据
X_train = [[1, 2], [2, 3], [3, 4]]  # 特征
y_train = [0, 1, 0]                  # 标签

# 训练模型
model.fit(X_train, y_train)

# 预测
prediction = model.predict([[4, 5]])
```

### 2. 无监督学习（Unsupervised Learning）

**定义**：使用无标签的数据进行训练

**任务类型**：
- **聚类（Clustering）**：发现数据中的模式
  - 客户分群
  - 图像分割
- **降维（Dimensionality Reduction）**：减少特征数量
  - PCA
  - t-SNE
- **关联规则**：发现数据间的关联
  - 购物篮分析

**示例**：
```python
# 无标签数据
X = [[1, 2], [2, 3], [10, 11], [11, 12]]

# 聚类
clusters = kmeans.fit(X)
# 结果：前两个点一组，后两个点一组
```

### 3. 强化学习（Reinforcement Learning）

**定义**：通过与环境交互学习最优策略

**核心概念**：
- **Agent**：智能体
- **Environment**：环境
- **Action**：动作
- **Reward**：奖励
- **Policy**：策略

**示例**：
```python
# Agent 在环境中行动
state = env.reset()
while not done:
    action = agent.choose_action(state)
    next_state, reward, done = env.step(action)
    agent.learn(state, action, reward, next_state)
    state = next_state
```

## 🔧 常见算法

### 1. 线性回归（Linear Regression）

```python
# 公式：y = wx + b
from sklearn.linear_model import LinearRegression

model = LinearRegression()
model.fit(X_train, y_train)
predictions = model.predict(X_test)
```

### 2. 逻辑回归（Logistic Regression）

```python
# 用于二分类
from sklearn.linear_model import LogisticRegression

model = LogisticRegression()
model.fit(X_train, y_train)
predictions = model.predict(X_test)
```

### 3. 决策树（Decision Tree）

```python
from sklearn.tree import DecisionTreeClassifier

model = DecisionTreeClassifier()
model.fit(X_train, y_train)
predictions = model.predict(X_test)
```

### 4. 随机森林（Random Forest）

```python
from sklearn.ensemble import RandomForestClassifier

model = RandomForestClassifier(n_estimators=100)
model.fit(X_train, y_train)
predictions = model.predict(X_test)
```

### 5. 支持向量机（SVM）

```python
from sklearn.svm import SVC

model = SVC(kernel='rbf')
model.fit(X_train, y_train)
predictions = model.predict(X_test)
```

## 📊 机器学习流程

### 1. 数据收集
- 收集相关数据
- 数据质量检查

### 2. 数据预处理
- 数据清洗
- 特征工程
- 数据标准化

### 3. 模型选择
- 根据任务选择算法
- 考虑数据特点

### 4. 模型训练
- 划分训练集和测试集
- 训练模型
- 调整超参数

### 5. 模型评估
- 评估指标（准确率、精确率、召回率等）
- 交叉验证

### 6. 模型部署
- 模型保存
- 部署到生产环境
- 监控和维护

## 💡 评估指标

### 分类任务

- **准确率（Accuracy）**：正确预测的比例
- **精确率（Precision）**：预测为正例中真正为正例的比例
- **召回率（Recall）**：真正例中被正确预测的比例
- **F1 分数**：精确率和召回率的调和平均

### 回归任务

- **均方误差（MSE）**：预测值与真实值的平方差
- **平均绝对误差（MAE）**：预测值与真实值的绝对差
- **R² 分数**：模型解释的方差比例

## 🔗 相关概念

- **特征工程**：选择和构造特征
- **过拟合**：模型在训练集上表现好，测试集上表现差
- **欠拟合**：模型在训练集和测试集上都表现差
- **交叉验证**：评估模型性能的方法
- **超参数调优**：调整模型参数

---

*最后更新：2024年*
